---
date created: 2026-04-22T00:00:00.000Z
tags:
  - 场景/部署
  - 场景
  - 部署服务器
  - 场景/工作流
related:
  - Mise
  - Docker
  - Linux
  - SSH
title: 部署服务器场景
date modified: '2026-07-31'
---

# 部署服务器场景

> 场景描述：从零开始部署一台生产服务器，配置环境并部署应用

## 问题背景

每次部署新服务器时，需要：

- 安装运行时环境（Node.js、Python 等）
- 配置 SSH 密钥
- 设置防火墙规则
- 部署应用并配置反向代理

## 解决方案

### 核心工具组合

| 工具/概念       | 作用       | 配置差异                    |
| --------------- | ---------- | --------------------------- |
| [[Mise]]        | 环境管理   | 使用 mise.toml 统一管理版本 |
| [[Git版本控制]] | 代码同步   | 使用 SSH 密钥认证           |
| Docker          | 容器化部署 | 参考 Docker部署流程         |
| Nginx           | 反向代理   | 统一配置模板                |

### 步骤 1：服务器初始化

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 创建部署用户
sudo adduser deploy
sudo usermod -aG sudo deploy
```

### 步骤 2：SSH配置

```bash
# 生成 SSH 密钥（本地）
ssh-keygen -t ed25519 -C "deploy@server"

# 复制公钥到服务器
ssh-copy-id deploy@server-ip

# 禁用密码登录（/etc/ssh/sshd_config）
PasswordAuthentication no
```

### 步骤 3：安装 Mise 并配置环境

```bash
# 安装 Mise
curl https://mise.run | sh

# 激活
echo 'eval "$(~/.local/bin/mise activate bash)"' >> ~/.bashrc
source ~/.bashrc

# 安装 Node.js
mise use --global node@lts

# 安装其他工具
mise use --global python@latest
```

### 步骤 4：部署应用

```bash
# 克隆代码
git clone git@github.com:user/repo.git
cd repo

# 安装依赖
mise install
npm install

# 构建
npm run build

# 启动服务（使用 systemd 或 PM2）
pm2 start npm --name "app" -- start
pm2 save
pm2 startup
```

### 步骤 5：配置 Nginx 反向代理

```nginx
# /etc/nginx/sites-available/app
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## 我的配置

### mise.toml 模板

```toml
[tools]
node = "22"
python = "3.12"

[env]
NODE_ENV = "production"
```

### PM2 配置文件

```javascript
// ecosystem.config.js
module.exports = {
  apps: [
    {
      name: "app",
      script: "npm",
      args: "start",
      env: {
        NODE_ENV: "production",
      },
    },
  ],
};
```

## 常见问题

### 问题 1：端口被占用

```bash
# 查看端口占用
sudo lsof -i :3000

# 杀死进程
sudo kill -9 <PID>
```

### 问题 2：权限问题

```bash
# 给部署用户权限
sudo chown -R deploy:deploy /var/www/app
```

### 问题 3：环境变量未生效

```bash
# 重新加载环境变量
source ~/.bashrc

# 或者在 PM2 中显式设置
pm2 start ecosystem.config.js
```

## 相关场景

- [[团队协作场景]] - 多人部署权限管理
- [[CI/CD 场景]] - 自动化部署

---

## 参考

- [[Mise]] - 环境管理工具
- [[Git版本控制]] - 代码版本管理
- [[HTTP协议]] - 反向代理原理
