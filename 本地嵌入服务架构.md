---
date created: '2026-06-三 11:12:01'
date modified: '2026-07-31'
tags:
  - skills
  - 技术/ai嵌入/服务
title: 本地嵌入服务架构
---

好问题，我来帮你设计这个产品架构。

核心思路：一拖多的微服务模式

┌──────────────────────────────────────────────────┐
│ 本地嵌入服务（一个进程） │
│ aha 或 轻量级 embedding server │
│ 占用 ~200MB 内存，只加载一个模型 │
│ 暴露 HTTP API 供所有客户端调用 │
│ 127.0.0.1:10101 │
└──────┬────────────┬──────────────┬────────────────┘
│ │ │
▼ ▼ ▼
Obsidian Obsidian CLI 工具
Vault A Vault B 批量处理
(工作笔记) (个人知识库) 定时任务

关键点：一个模型服务跑着，多个 Obsidian 仓库、多个工具都能同时用，不冲突，不重复加载。

---

产品设计

产品名：aha-classify（作为 aha 生态的一环）

分层架构

第 0 层：嵌入服务

就用 aha 现有的嵌入端点，或做一个更专用的精简版：

# 方式一：直接用 aha

aha serv -m sentence-transformers/all-MiniLM-L6-v2 -p 10101

# 方式二：做一个专用的 aha-classify server（更瘦）

aha-classify serve --model all-MiniLM-L6-v2 --port 10101

暴露两个接口：

┌─────────────────────┬───────────────────────────────────────────────────┐
│ 接口 │ 作用 │
├─────────────────────┼───────────────────────────────────────────────────┤
│ POST /v1/embeddings │ 通用的文本转向量 │
├─────────────────────┼───────────────────────────────────────────────────┤
│ POST /v1/classify │ 高级接口：传分类规则 + 笔记列表，直接返回归类结果 │
└─────────────────────┴───────────────────────────────────────────────────┘

这个 /v1/classify 是核心创新点——服务端一次性算好相似度，客户端只需要拿结果。

POST /v1/classify
{
"categories": {
"01-编程": "Rust、Python、框架、代码、技术方案、bug...",
"02-AI": "机器学习、大模型、提示词、AI工具...",
"03-项目": "Sprint、需求、会议、周报、复盘..."
},
"documents": [
{"id": "note1", "text": "Rust 所有权系统详解..."},
{"id": "note2", "text": "Qwen3 部署踩坑记录..."}
],
"threshold": 0.6 // 低于此值归入"待分类"
}

→ 返回
{
"results": [
{"id": "note1", "category": "01-编程", "score": 0.92},
{"id": "note2", "category": "02-AI", "score": 0.88},
],
"unclassified": [] // 低于阈值的笔记
}

这样做的好处：

- 计算在服务端，所有客户端共享缓存（同一篇笔记不用重复算向量）
- 客户端极薄，Obsidian 插件就几十行逻辑
- 分类规则集中管理，换个规则不用更新插件

---

第 1 层：Obsidian 插件

插件里做的事情很少：

1. 用户打开 Obsidian → 插件检测本地服务是否运行
   ↓ 没运行 → 提示"一键启动"（或随 Obsidian 自启）

2. 用户在插件设置里定义"分类方案"
   ┌────────────────────────────┐
   │ 分类方案: 工作笔记 │
   │ ├─ 01-编程: [描述] │
   │ ├─ 02-AI: [描述] │
   │ ├─ 03-项目: [描述] │
   │ └─ 04-生活: [描述] │
   │ │
   │ 分类方案: 个人知识库 │
   │ ├─ 01-阅读笔记 │
   │ ├─ 02-写作碎片 │
   │ ├─ 03-健康记录 │
   │ └─ 04-收藏 │
   └────────────────────────────┘
   ↑ 不同 vault 用不同方案，在插件里一键切换

3. 点击"预览分类"
   → 插件把所有笔记发到本地服务
   → 服务返回归类结果
   → 插件在 Obsidian 里展示:
   📂 01-编程 (12篇)
   ├── Rust所有权系统.md ★0.92
   ├── Python异步编程.md ★0.88
   └── ...
   📂 02-AI (8篇)
   ├── Qwen3部署.md ★0.91
   └── ...
   📂 待分类 (2篇) ← 用户手动处理

4. 用户确认 → 一键执行移动
   插件帮你搬文件，Obsidian [[链接]] 自动跟着走

插件 UI 设计：

┌─────────────────────────────────────────┐
│ 🔄 连接到本地服务 ✓ (all-MiniLM-L6-v2) │
├─────────────────────────────────────────┤
│ 分类方案: 工作笔记 [切换 ▼] │
│ │
│ [🔍 预览分类] [⚡ 执行整理] [↩ 撤销] │
│ │
│ 📂 01-编程 12 篇 相似度 │
│ 📂 02-AI 8 篇 0.91 │
│ 📂 03-项目 5 篇 0.88 │
│ 📂 04-生活 3 篇 0.85 │
│ ❓ 待分类 2 篇 < 0.60 │
│ │
│ ⚙️ 阈值: 0.60 ──────●────────── 1.00 │
└─────────────────────────────────────────┘

---

第 2 层：CLI 工具

用于批量操作、定时任务、CI 流程：

# 一次性整理

aha-classify run --vault ./my-obsidian --scheme work

# 查看分类预览（不实际移动）

aha-classify preview --vault ./my-obsidian --scheme work

# 交互式确认

aha-classify review --vault ./my-obsidian

# 只整理新增的笔记（增量模式）

aha-classify run --vault ./my-obsidian --incremental

# 查看上一次整理的报告

aha-classify log

---

为什么这样设计"不冲突、省资源"

┌───────────────────────────────┬───────────────────────────────┬──────────────────────────────┐
│ 方案 │ 资源占用 │ 冲突风险 │
├───────────────────────────────┼───────────────────────────────┼──────────────────────────────┤
│ ❌ 每个插件自己加载模型 │ 每开一个 vault 多吃 ~200MB │ 多实例抢 GPU 显存 │
├───────────────────────────────┼───────────────────────────────┼──────────────────────────────┤
│ ❌ 每个项目跑一个服务 │ 内存爆炸 │ 端口冲突 │
├───────────────────────────────┼───────────────────────────────┼──────────────────────────────┤
│ ✅ 一个嵌入服务，多客户端共享 │ 固定 ~200MB，不管多少个 vault │ 一个端口，一个模型，一次加载 │
└───────────────────────────────┴───────────────────────────────┴──────────────────────────────┘

对比 aha 的关系：

aha 本身: 全能推理引擎（LLM + VLM + OCR + ASR + Embedding）
aha-classify: aha 生态中的一个专注工具 - 复用一个嵌入模型 - 只加了一层分类逻辑 - 提供 Obsidian 插件 + CLI 两种入口

---

技术栈建议

┌───────────────┬────────────────────────────────────┬────────────────────────────────────┐
│ 层 │ 技术 │ 理由 │
├───────────────┼────────────────────────────────────┼────────────────────────────────────┤
│ 嵌入服务 │ Rust + candle（复用 aha 现有代码） │ 性能好，内存低，不需要 Python 环境 │
├───────────────┼────────────────────────────────────┼────────────────────────────────────┤
│ Obsidian 插件 │ TypeScript │ Obsidian 插件生态标准 │
├───────────────┼────────────────────────────────────┼────────────────────────────────────┤
│ CLI │ Rust（嵌入式服务自带的命令行） │ 一行命令搞定 │
└───────────────┴────────────────────────────────────┴────────────────────────────────────┘

Obsidian 插件和服务的通信用 fetch() 调 localhost:10101，不需要任何额外依赖。

---

最小可行产品（MVP）路线

第一阶段（可用的最小闭环）：

1. aha 增加 /v1/classify 端点（已有 /v1/embeddings，加一个分类逻辑不难）
2. 写一个 Python/Shell 脚本做 CLI 版验证
3. 验证效果：输入一个 OB 仓库 + 分类规则 → 输出归类结果

第二阶段：4. 写 Obsidian 插件（调用本地服务，展示结果，执行移动）5. 插件支持多 vault 和方案切换

第三阶段：6. 做增量处理（只算变更过的笔记）7. 支持 Obsidian 的 [[链接]] 自动更新 8. 嵌入启动图标到系统托盘（一键启停服务）

---

你觉得这个方向对吗？如果对的话，要不要先从第一阶段开始——我帮你在 aha 里加一个 /v1/classify 端点？这样你马上就能用 curl
或者写几行脚本验证分类效果，不用等插件做好。
