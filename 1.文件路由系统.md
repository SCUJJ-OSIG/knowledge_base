---
date created: '2026-04-日 18:55:00'
date modified: '2026-07-31'
tags:
  - 全栈
  - 技术/架构/文件路由
  - tanstack
  - 技术/前端/路由
title: TanStack Router 文件路由系统
---

**TanStack Router 的文件路由（File-Based Routing）**

---

## 一、核心概念

文件路由 = **目录结构 = URL 结构 = 路由嵌套关系**。

- 约定目录：默认 `src/routes/`

- 文件名 / 目录名直接映射 URL path

- 自动生成类型与路由树（`routeTree.gen.ts`）

- 一次配置，全局类型安全

---

## 二、基础命名规则（最常用）

### 1. 首页 / 根路由

- 文件：`src/routes/index.tsx`

- URL：`/`

```tsx
// src/routes/index.tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/")({
  component: () => <h1>首页</h1>,
});
```

### 2. 普通页面

- 文件：`src/routes/about.tsx`

- URL：`/about`

### 3. 嵌套路由（目录）

- 目录：`src/routes/blog/`

- 目录下：`index.tsx` → `/blog`

- 子页：`src/routes/blog/posts.tsx` → `/blog/posts`

### 4. 动态路由（路径参数）

- 文件：`src/routes/blog/$postId.tsx`

- URL：`/blog/123`

- 自动生成 `params: { postId: string }` 类型

```tsx
// src/routes/blog/$postId.tsx
export const Route = createFileRoute("/blog/$postId")({
  loader: ({ params }) => fetchPost(params.postId),
  component: ({ params }) => <div>文章：{params.postId}</div>,
});
```

### 5. 布局路由（父路由）

- 父文件：`src/routes/blog.tsx`（无 index）

- 内部用 `<Outlet />` 渲染子路由

```tsx
// src/routes/blog.tsx
export const Route = createFileRoute("/blog")({
  component: () => (
    <div>
      <h1>博客布局</h1>
      <Outlet /> {/* 子路由在这里渲染 */}
    </div>
  ),
});
```

结构效果：

```Plain
/blog          → blog.tsx + blog/index.tsx
/blog/123      → blog.tsx + blog/$postId.tsx
```

### 6. 404 路由

- 文件：`src/routes/$$.tsx`（**双美元**）

- 匹配所有未匹配路径

### 7. 路由组（分组不影响 URL）

- 目录：`(marketing)/about.tsx`

- URL 仍为 `/about`，仅用于文件组织

8. 下划线 `_` 开头的文件/文件夹,核心作用是「隐藏路由」—— 仅作为共享组件/布局/工具
   属于官方约定的「非路由文件/目录,不直接映射为 URL 路径，属于官方约定的「非路由文件 / 目录」

---

## 三、完整目录示例（一眼看懂）

```Plain
src/routes/
├── index.tsx                → /
├── about.tsx                → /about
├── blog.tsx                 → /blog（布局）
├── blog/
│   ├── index.tsx            → /blog
│   ├── $postId.tsx          → /blog/:postId
│   └── posts.tsx            → /blog/posts
├── (auth)/
│   ├── login.tsx            → /login
│   └── register.tsx         → /register
├── dashboard/
│   ├── index.tsx            → /dashboard
│   └── settings.tsx         → /dashboard/settings
└── $$.tsx                   → 404
```

---

## 四、核心 API（文件路由专用）

### 1. createFileRoute

每个路由文件**必须导出** **`Route`**：

```tsx
import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/路径")({
  component: PageComponent, // 页面组件
  loader: async ({ params }) => {}, // 数据加载（SSR/SSG）
  beforeLoad: () => {}, // 权限校验
  errorComponent: ErrorCmp, // 错误边界
  pendingComponent: LoadingCmp, // 加载态
});
```

### 2. 自动生成路由树

- 插件（Vite/Webpack）扫描 `routes/`

- 生成 `src/routeTree.gen.ts`

- 所有导航、link 全**类型安全**：

```tsx
// 自动提示路径、必填 params
navigate({ to: '/blog/$postId', params: { postId: '123' } })

// <Link /> 同样类型安全
<Link to="/blog/$postId" params={{ postId: '123' }}>文章</Link>
```

---

## 五、Vite 配置（最简）

```ts
// vite.config.ts
import { tanstackRouterPlugin } from "@tanstack/router-plugin/vite";

export default {
  plugins: [
    tanstackRouterPlugin({
      routesDirectory: "./src/routes",
      generatedRouteTree: "./src/routeTree.gen.ts",
    }),
  ],
};
```

---

## 六、和 Next.js/ Remix 文件路由的区别

- **TanStack**：文件名 / 目录名即路径，`$param` 动态段，**强类型、无魔法**

- **Next.js**：`[param]` 动态段，`(group)` 路由组，有更多全栈约定

- **Remix**：类似 Next，但更偏后端数据加载

---

## 七、适合你的场景（和你之前需求呼应）

- **本地组件 /skill 库**：把常用页面 / 组件放在 `routes/` 或共享目录

- **中英双语 / 软链接思路**：文件结构统一，**改一处全局生效**（类型 + 路由 + 组件）

- **新项目复用**：直接软链接或复制 `routes/` + 共享组件，**不用重新安装**

- **TS 类型统一**：所有路由、参数、loader 返回值**自动同步**，无拼写错误

---

## 七、一句话总结

TanStack 文件路由 = **目录即路由 + 全自动类型安全 + 内置数据 / 缓存 / 布局**，是大型 TS 项目最简洁、最可靠的路由方案。

---

要不要我根据你的技能库（skills）结构，帮你生成一套可直接复制的 **中文命名 + 英文路径** 的 TanStack 路由目录和文件模板？
