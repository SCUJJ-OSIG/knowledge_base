---
date created: 2026-07-30
date modified: 2026-07-31
tags: [元/规则, 元/宪法]
aliases: [知识库宪法, 分类规则, KB-Rules]
version: 2.0
status: 生效中
---

# 知识库管理规则(宪法)

> ⚠️ 这是知识库的**单一事实源**。所有笔记的组织方式以本文件为准。
> 修改本文件必须遵守文末的「修订约束」——不轻易改规则。

---

## 一、第一性原则

**个人知识库的最大成本是「归类决策」,不是「内容重复」。**

一切规则以「降低归类与维护成本」为最高目标。当某条规则增加了决策负担却没带来对等收益时,该规则本身就是错的。

> **v2.0 新增**:`归类决策` 的最大成本是**目录嵌套**——每多一层目录,就要多回答一次"放哪"。v2.0 改用 frontmatter 字段分类,目录仅作为"教程集合"的物理容器。

---

## 二、笔记的两种形态(最重要的前置判断)

动手归类前,先问一句:**这段是「参考」还是「教程」?**

| 形态 | 特征 | 例子 | 是否独立成文 |
|------|------|------|------------|
| **参考型** | 定义、速查、命令、配置片段。查完即走,追求精简 | HTTP 方法表、Mise 配置、Git 命令 | ✅ 可独立 |
| **教程型** | 长文、推导、学习路径。价值在于**连贯**,拆开即毁 | 网络通信完整教程、架构思考长文 | ✅ 必须独立 |

**核心结论**:v2.0 起,两种形态都**独立成文**,不再有"体用嵌入/拆分"。

---

## 三、目录结构规则(2026-07-31 扁平化)

### 3.1 vault 根(扁平化主区)

**绝大多数 .md 直接放在 vault 根**,不再按"体/用/场景"嵌套子目录。

```
vault 根/
├── HTTP协议详解与网络通信基础.md    ← 教程型
├── Mise：下一代一站式开发环境管理工具.md  ← 工具参考
├── 编程必备.md                   ← 博客底稿
└── 知识库管理规则.md              ← 宪法
```

### 3.2 保留的子目录(系统教程/有图)

只保留两类:

1. **系统教程集合** — 同一主题的系列教程(如 TS/JS/CSS 学习树)
2. **有图片资源** — 单个 .md 独立子目录 + `assets/`,或批量教学图组

| 保留目录 | 类型 |
|---------|------|
| `2-技术文章/02-TypeScript全栈开发/` | TS/JS/CSS/浏览器系统教程(100+ 篇) |
| `2-技术文章/05-DevOps与基础设施/Linux系统管理/` | 服务器运维教程 |
| `2-技术文章/{0-vim,1-aiCoding,组件封装}/` | 工具/设计教程 |
| `2-技术文章/08-AI编程/ai+自动化/` | AI 编程教程 |
| `2-技术文章/05-DevOps与基础设施/{容器化技术,CI-CD流水线}/` | 运维教程 |
| `2-技术文章/{Git版本控制,12-磁盘}/` | 含图教程 |
| `4-全栈/01-elysia/` | Elysia 系统(25 篇 + 图) |
| `7-信息研究/金钱流动/` | 含图笔记 |
| `<md-name>/` | 单个有图 .md 独立子目录 |

### 3.3 元文件(永久保留根目录)

```
CLAUDE.md                # AI 协作指引
README.md                # 规则总览
知识库管理规则.md         # 本文件(宪法)
views.published.base     # 网站发布视图
```

### 3.4 严禁

- ❌ 不要新建 `0-体/`、`1-自用软件/`、`3-场景/` 这种分类目录
- ❌ 不要为场景、形态建目录(用 frontmatter 字段)
- ❌ 不要超过 2 层子目录嵌套(扁平化精神)
- ❌ 不要让多篇 .md 共享一个 `assets/` 目录

---

## 四、frontmatter 分类(替代目录分类)

### 4.1 必填字段

```yaml
---
形态: 教程型          # 参考型 | 教程型(必填)
tags: [网络, 教程]     # 自由标签(必填)
场景: 部署            # 自由场景名(选填)
published: true       # 是否发到 my-blog(选填,默认不发)
series: HTTP          # 系列名(选填,系列教程用)
---
```

### 4.2 字段语义

| 字段 | 用途 | 网站体现 |
|------|------|---------|
| `形态` | `参考型` / `教程型` | 形态 chip 筛选 |
| `tags` | 自由字符串数组 | tags chip 筛选 |
| `场景` | 自由字符串 | 场景 chip 筛选 |
| `published` | `true` 才发网站 | 数据库过滤 |
| `series` | 系列名 | 系列 chip 筛选 |

### 4.3 命名规则

- vault 根 .md 直接用标题名,不再强制 `[编号]-[标题]`
- 数字前缀(`0-`、`1-`、`2-`)剥掉,不参与分类
- 同名冲突:加源目录前缀(如 `0-blog--未命名.md`)

---

## 五、配合 my-blog 网站发布(2026-07-31 新增)

### 5.1 发布流程

```
[本 vault] → git push → GitHub → Octokit 拉取 → gray-matter 解析
            → only published === true → 渲染成 posts/<slug>/
            → 每天 0 点(UTC) GitHub Action 自动构建 → 部署 GitHub Pages
```

### 5.2 唯一开关:`published: true`

**没写 `published` 或 `published: false` 都不发网站**。要批量发布:用 `scripts/` 下的脚本。

### 5.3 视图配置:`views.published.base`

改这一个文件 = 改整个网站首页的 chip 栏结构(形态/场景/tags/系列)。改完 commit,push,下次自动构建生效。

---

## 六、图片资源规则

### 6.1 有图的 .md → 独立同名子目录

```
电脑相关权限问题/
├── 电脑相关权限问题.md
└── assets/
    └── image-20231111200427031.png
```

`.md` 里引用:

```markdown
![[image-20231111200427031.png]]            ← Obsidian 嵌入(推荐)
![alt](image-20231111200427031.png)         ← 标准 markdown 也能用
```

### 6.2 系统教程里的大图组

保留原结构,不动(如 `vue3ts尚硅谷/images/` 几十张教学图)。

### 6.3 严禁

- ❌ vault 根放全局 `assets/`
- ❌ 多篇 .md 共享 assets
- ❌ `![](./相对路径/assets/...)` 跨多级目录

---

## 七、wikilink 引用规则

| 语法 | 行为 | 用法 |
|------|------|------|
| `![[文件名]]` | **嵌入** | 图片、复用整段内容 |
| `[[文件名]]` | **跳转** | 文章间的引用 |

- **默认用嵌入**(`![[]]`),除非明确只是"参见"
- 优先用**文件名**,不要写路径(搬了家也能找到)

---

## 八、去重原则

1. **同一概念只写一份**。如果要写第二份,先想清楚是不是不同的"形态"(参考 vs 教程)。
2. **不同形态可以共存**:参考型(速查)和教程型(长文)各自独立。
3. 判定"是否重复":两份文件 > 70% 内容重叠才算重复,否则保留。
4. **绝不要为"将来会引用"提前抽离**。等第 3 次要抄同一段时才合并。

---

## 九、修订约束(不轻易改规则)

1. 本文件为**宪法级**,非经明确复盘不得修改。
2. 每次修改必须:
   - 递增 `version`(如 1.0 → 1.1 → 2.0)
   - 在文末「修订记录」写明**改了什么、为什么改、被什么证据推翻**
3. 规则以**实测证据**为准,不以直觉为准。任何规则被真实案例证伪时,先记录反例,再改规则。

---

## 修订记录

- **v1.0(2026-07-30)** 初版。基于对本仓库实测得出:
  - HTTP 案例证明「参考 vs 教程」是比「体 vs 用」更根本的区分——两份 HTTP 文件(131 行速查 + 429 行教程)并非重复,而是两种形态,故不应合并。
  - 178 个 md 仅 41 个用双链、`0-体`/`3-场景` 多为空模板——证明三维归类的决策成本会劝退维护,故场景层改为 tag + Dataview。
  - `[[]]` 无法「一改全改」,只有 `![[]]` 可以——故用层引用体统一用嵌入。

- **v1.1(2026-07-30)** 重大修正:
  - **目录命名规范(`0-体/`、`1-自用软件/`、`2-技术文章/`)作废**。目录只服务于 vault 内部浏览,不再承担任何分类/网站路由职责。
  - **分类从"路径"改为 frontmatter 字段**:`形态`(参考型/教程型)、`场景`(如部署/网络/团队协作)、`published`(是否发到网站)。
  - **新增网站发布配置文件 `views.base`**,位于 vault 根目录。定义网站 tab 栏结构、过滤条件、显示字段。**网站侧只读这一个文件**,vault 改 frontmatter 即同步网站。
  - 废弃依据:原 `0-体/` 等目录命名本质上仍是"用路径表达分类"——而分类本质是 frontmatter 字段,不是路径。两个方案并存导致"在哪个目录"的归类决策反复出现。

- **v2.0(2026-07-31)** 根本性推翻 v1.0:
  - **删除整个 `0-体/` 目录**(7 个 .md 全部移除),**删除体用双链架构**。原因:
    - **体用思想维护成本过高**:每写一篇笔记都要回答"这是体还是用",在 0-体/ 和 1-自用软件/ 之间反复决策,劝退维护。
    - **v1.0 时承诺的"一改全改"**从未真正用上——`![[]]` 嵌入在编辑模式不可见,导出到博客失效(博客用标准 markdown 渲染)。
    - **两份 HTTP 文件(参考 vs 教程)本来就是独立内容**,不需要"体用"概念来区分,只需 frontmatter `形态` 字段就够。
  - **彻底扁平化**:47 篇 .md 从子目录移到 vault 根(数字前缀剥掉,同名加源目录前缀)。
  - **有图的 .md → 独立同名子目录 + `assets/`**(3 个,用户验证这是正确做法)。
  - **保留 7 个系统教程/有图子目录**:TS 学习树(100+ 篇)、Linux 教程、elysia、含图教程等。
  - **8 篇 `未命名*.md` 按内容改名**:`未命名.md` → `知识管理-体用双链架构(历史).md`、`未命名 1.md` → `知识本体引擎-Rust架构.md` 等。
  - **新增 `my-blog` 协作章节**:发布流程、`published` 唯一开关、`views.published.base` 视图配置。
  - 废弃依据:
    - 实测显示,只跑 1 天,`0-体/` 7 篇笔记里 6 篇内容是"参考型定义",跟 `1-自用软件/` 里的教程/配置大量重复——分类未带来对等收益。
    - 用户明确反馈"体用思想难以达到",决定永久废弃。
