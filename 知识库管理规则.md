---
date created: 2026-07-30
date modified: 2026-07-30
tags: [元/规则, 元/宪法]
aliases: [知识库宪法, 分类规则, KB-Rules]
version: 1.1
status: 生效中
---

# 知识库管理规则（宪法）

> ⚠️ 这是知识库的**单一事实源**。所有笔记的组织方式以本文件为准。
> 修改本文件必须遵守文末的「修订约束」——不轻易改规则。

---

## 一、第一性原则

**个人知识库的最大成本是「归类决策」，不是「内容重复」。**

一切规则以「降低归类与维护成本」为最高目标。当某条规则增加了决策负担却没带来对等收益时，该规则本身就是错的。

---

## 二、笔记的两种形态（最重要的前置判断）

动手归类前，先问一句：**这段是「参考」还是「教程」？**

| 形态 | 特征 | 例子 | 归属 |
|------|------|------|------|
| **参考型** | 定义、速查、命令、配置片段。查完即走，追求精简 | HTTP 方法表、Mise 配置、Git 命令 | → 进「体」，可被 `![[]]` 嵌入，适用体用分离 |
| **教程型** | 长文、推导、学习路径。价值在于**连贯**，拆开即毁 | 网络通信完整教程、架构思考长文 | → **原地保留**，只打 tag，**禁止拆分/嵌入** |

**核心结论**：体用分离与 `![[]]` 嵌入机制，**只适用于参考型笔记**。教程型笔记天生独立，不进这套体系。

---

## 三、体（Essence）层规则

**定义**：跨场景复用的概念/工具/技能的本质定义。位于 `0-体/`。

### 3.1 何时拆出「体」——事后触发，不预判

> 不要在写笔记时预判「这将来会被引用几次」——当下判断不了。

**触发条件**：当你**第 3 次要抄同一段参考内容**时，那一刻才把它抽成「体」。
让重复的痛感驱动重构，而不是提前设计。

**判断标准**：拆出去能省的重复成本 > 跳转 + 维护成本，才拆。

### 3.2 体文件只写「不变的本质」

- 只写定义、原理、核心机制——不随时间/个人配置变化的部分。
- **不写**个人配置、不写场景用法（那些属于「用」层）。

---

## 四、用（Application）层规则

**定义**：场景化的个人配置与用法。位于 `1-自用软件/`、`2-技术文章/`、`4-全栈/` 等。

- 引用「体」用 **`![[体名#章节]]` 嵌入**，**不用** `[[体名]]` 跳转。
  - 原因：`[[]]` 是跳转，改了体不会同步；只有 `![[]]` 嵌入才能实现「一改全改」。
- 用层只写**与默认配置的差异**和**个人化用法**，不重复体层的定义。

---

## 五、场景（Scene）层规则

**场景是查询视角，不是存储结构。**

- **禁止**为场景建实体目录（不再维护 `3-场景/` 下的手动文件）。
- 用 **tag + Dataview** 动态生成：给相关笔记打 `#场景/部署`、`#场景/协作` 等标签，用一个 Dataview 查询表聚合。
- 好处：省掉一整个归类维度，且不产生孤立文件。

---

## 六、`![[]]` 嵌入机制的已知代价（接受后再用）

1. 嵌入块**只在阅读模式渲染**，编辑时看到的是一行 `![[...]]`，写作体验会割裂。
2. 该语法**仅 Obsidian 生态支持**。导出到其他工具或发博客时，嵌入会失效。

—— 能接受这两点，才使用嵌入；否则该内容就直接内联写死。

---

## 七、决策流程图（照着做，不凭感觉）

```
新增/整理一段知识
      │
      ▼
是「参考型」还是「教程型」？
      │
 ┌────┴─────┐
 教程型      参考型
 │            │
 原地保留     是否已被抄到第 3 次？
 只打 tag       │
 结束      ┌────┴────┐
           否        是
           │         │
        写在用层   抽成「体」
        内联即可   用层改用 ![[体#章节]] 嵌入
```

---

## 八、修订约束（不轻易改规则）

1. 本文件为**宪法级**，非经明确复盘不得修改。
2. 每次修改必须：
   - 递增 `version`（如 1.0 → 1.1）；
   - 在文末「修订记录」写明**改了什么、为什么改、被什么证据推翻**。
3. 规则以**实测证据**为准，不以直觉为准。任何规则被真实案例证伪时，先记录反例，再改规则。

---

## 修订记录

- **v1.0（2026-07-30）** 初版。基于对本仓库实测得出：
  - HTTP 案例证明「参考 vs 教程」是比「体 vs 用」更根本的区分——两份 HTTP 文件（131 行速查 + 429 行教程）并非重复，而是两种形态，故不应合并。
  - 178 个 md 仅 41 个用双链、`0-体`/`3-场景` 多为空模板——证明三维归类的决策成本会劝退维护，故场景层改为 tag + Dataview。
  - `[[]]` 无法「一改全改」，只有 `![[]]` 可以——故用层引用体统一用嵌入。
- **v1.1（2026-07-30）** 重大修正：
  - **目录命名规范（`0-体/`、`1-自用软件/`、`2-技术文章/`）作废**。目录只服务于 vault 内部浏览方便，不再承担任何分类/网站路由职责。
  - **分类从"路径"改为 frontmatter 字段**：`形态`（参考型/教程型）、`场景`（如部署/网络/团队协作）、`published`（是否发到网站）。
  - **新增网站发布配置文件 `views.base`**，位于 vault 根目录。定义网站 tab 栏结构、过滤条件、显示字段。**网站侧只读这一个文件**，vault 改 frontmatter 即同步网站。
  - 废弃依据：原 `0-体/` 等目录命名本质上仍是"用路径表达分类"——而分类本质是 frontmatter 字段，不是路径。两个方案并存导致"在哪个目录"的归类决策反复出现。
