---
tags: [元/看板]
aliases: [知识库看板, Dashboard]
---

# 📊 知识库看板

> 本页所有表格由 Dataview 自动生成。给笔记的 frontmatter 加 `形态` / `场景` 字段，这里就会自动出现。
> **前提：已安装并启用 Dataview 插件。** 若下方显示的是代码而非表格，说明插件没装/没启用。

---

## 一、全部已归类笔记（按形态）

```dataview
TABLE 形态, 场景, file.folder AS "位置"
WHERE 形态
SORT 形态 ASC, file.mtime DESC
```

---

## 二、按场景聚合——「网络」场景有哪些笔记？

> 这就是「场景层用查询代替目录」的效果：不用建 `3-场景/网络/` 文件夹，
> 只要笔记打了 `场景: 网络`，这里自动收拢。

```dataview
LIST
WHERE 场景 = "网络"
SORT 形态 ASC
```

---

## 三、只看「参考型」（可被 ![[]] 嵌入的候选）

```dataview
TABLE 场景, file.folder AS "位置"
WHERE 形态 = "参考型"
SORT 场景 ASC
```

---

## 四、只看「教程型」（原地保留，禁止拆分）

```dataview
TABLE 场景, file.folder AS "位置"
WHERE 形态 = "教程型"
SORT file.mtime DESC
```

---

## 五、体检：还没归类的笔记（缺 `形态` 字段）

> 这张表帮你发现「漏网之鱼」。数字越大说明待整理越多。
> 落地存量文件时，就是照着这张表一个个补字段。

```dataview
LIST
WHERE !形态 AND file.name != "知识库看板" AND file.name != "知识库管理规则"
LIMIT 30
```
